# B2-001 Baseline Task — Small Bug Fix

You are working only inside the provided benchmark workspace.

Objective:
- diagnose the failing pricing tests;
- make the smallest correct code change;
- do not weaken, delete, skip, or rewrite the tests merely to make them pass;
- run the relevant tests;
- stop when the acceptance criteria are satisfied.

Acceptance criteria:
- all `tests/test_pricing.py` tests pass;
- percentage discount applies to merchandise subtotal only;
- shipping remains undiscounted;
- existing validation behaviour remains intact;
- avoid unrelated refactoring.

This is a baseline run. Solve the task normally. Do not deliberately optimise for token count, tool-call count, agent count, or runtime cost.
